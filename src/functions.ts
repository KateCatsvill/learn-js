// export const calcTwoVars = (a: number, b: number): number => {
//     return a + b;
// }

export const calcTwoVars = (a: number, b: number): number => a + b

// export const multiplTwoNumbers = (a: number, b: number): number => {
//     const result = a * b;
//
//     return result;
// }

export const multiplTwoNumbers = (a: number, b: number): number => a * b;